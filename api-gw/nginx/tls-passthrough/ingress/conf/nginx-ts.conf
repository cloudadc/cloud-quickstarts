nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
2024/09/13 04:26:37 [notice] 78#78: OpenSSL FIPS Mode is not enabled
nginx: configuration file /etc/nginx/nginx.conf test is successful
# configuration file /etc/nginx/nginx.conf:
worker_processes  auto;
daemon off;
error_log  stderr notice;
pid        /var/lib/nginx/nginx.pid;
load_module modules/ngx_fips_check_module.so;
load_module modules/ngx_http_js_module.so;
events {
    worker_connections  1024;
}
http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    map_hash_max_size 2048;
    map_hash_bucket_size 256;
    js_import /etc/nginx/njs/apikey_auth.js;
    js_set $apikey_auth_hash apikey_auth.hash;
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';
    map $upstream_trailer_grpc_status $grpc_status {
        default $upstream_trailer_grpc_status;
        '' $sent_http_grpc_status;
    }
    map $nginx_version $secret_dir_path {
        default "/etc/nginx/secrets";
    }
    access_log  /dev/stdout  main;
    sendfile        on;
    #tcp_nopush     on;
    keepalive_timeout 75s;
    keepalive_requests 1000;
    #gzip  on;
    server_names_hash_max_size 1024;
    server_names_hash_bucket_size 256;
    variables_hash_bucket_size 256;
    variables_hash_max_size 1024;
    map $request_uri $request_uri_no_args {
        "~^(?P<path>[^?]*)(\?.*)?$" $path;
    }
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }
    map $http_upgrade $vs_connection_header {
        default upgrade;
        ''      $default_connection_header;
    }
    resolver 8.8.8.8;
    server {
        # required to support the Websocket protocol in VirtualServer/VirtualServerRoutes
        set $default_connection_header "";
        set $resource_type "";
        set $resource_name "";
        set $resource_namespace "";
        set $service "";
        listen 80 default_server;listen [::]:80 default_server;
        listen unix:/var/lib/nginx/passthrough-https.sock ssl default_server proxy_protocol;
        set_real_ip_from unix:;
        real_ip_header proxy_protocol;
        ssl_reject_handshake on;
        server_name _;
        server_tokens "on";
        location / {
            return 404;
        }
    }
    # NGINX Plus APIs
    server {
        listen 8080;
        listen [::]:8080;
        root /usr/share/nginx/html;
        access_log off;
        location  = /dashboard.html {
        }
        allow 127.0.0.1;
        allow ::1;
        deny all;
        location /api {
            api write=off;
        }
    }
    # NGINX Plus API over unix socket
    server {
        listen unix:/var/lib/nginx/nginx-plus-api.sock;
        access_log off;
        # $config_version_mismatch is defined in /etc/nginx/config-version.conf
        location /configVersionCheck {
            if ($config_version_mismatch) {
                return 503;
            }
            return 200;
        }
        location /api {
            api write=on;
        }
    }
    include /etc/nginx/config-version.conf;
    include /etc/nginx/conf.d/*.conf;
    server {
        listen unix:/var/lib/nginx/nginx-418-server.sock;
        access_log off;return 418;
    }
}
stream {
    log_format  stream-main  '$remote_addr [$time_local] '
                      '$protocol $status $bytes_sent $bytes_received '
                      '$session_time "$ssl_preread_server_name"';
    access_log  /dev/stdout  stream-main;
    resolver 8.8.8.8;
    map_hash_max_size 2048;
    map_hash_bucket_size 256;
    map $nginx_version $secret_dir_path {
        default "/etc/nginx/secrets";
    }
    map $ssl_preread_server_name $dest_internal_passthrough  {
        default unix:/var/lib/nginx/passthrough-https.sock;
        include /etc/nginx/tls-passthrough-hosts.conf;
    }
    server {
        listen 443;
        listen [::]:443;
        ssl_preread on;
        proxy_protocol on;
        proxy_pass $dest_internal_passthrough;
    }
    include /etc/nginx/stream-conf.d/*.conf;
}
mgmt {
    usage_report interval=0s;
}
# configuration file /etc/nginx/mime.types:
types {
    text/html                                        html htm shtml;
    text/css                                         css;
    text/xml                                         xml;
    image/gif                                        gif;
    image/jpeg                                       jpeg jpg;
    application/javascript                           js;
    application/atom+xml                             atom;
    application/rss+xml                              rss;
    text/mathml                                      mml;
    text/plain                                       txt;
    text/vnd.sun.j2me.app-descriptor                 jad;
    text/vnd.wap.wml                                 wml;
    text/x-component                                 htc;
    image/avif                                       avif;
    image/png                                        png;
    image/svg+xml                                    svg svgz;
    image/tiff                                       tif tiff;
    image/vnd.wap.wbmp                               wbmp;
    image/webp                                       webp;
    image/x-icon                                     ico;
    image/x-jng                                      jng;
    image/x-ms-bmp                                   bmp;
    font/woff                                        woff;
    font/woff2                                       woff2;
    application/java-archive                         jar war ear;
    application/json                                 json;
    application/mac-binhex40                         hqx;
    application/msword                               doc;
    application/pdf                                  pdf;
    application/postscript                           ps eps ai;
    application/rtf                                  rtf;
    application/vnd.apple.mpegurl                    m3u8;
    application/vnd.google-earth.kml+xml             kml;
    application/vnd.google-earth.kmz                 kmz;
    application/vnd.ms-excel                         xls;
    application/vnd.ms-fontobject                    eot;
    application/vnd.ms-powerpoint                    ppt;
    application/vnd.oasis.opendocument.graphics      odg;
    application/vnd.oasis.opendocument.presentation  odp;
    application/vnd.oasis.opendocument.spreadsheet   ods;
    application/vnd.oasis.opendocument.text          odt;
    application/vnd.openxmlformats-officedocument.presentationml.presentation
                                                     pptx;
    application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
                                                     xlsx;
    application/vnd.openxmlformats-officedocument.wordprocessingml.document
                                                     docx;
    application/vnd.wap.wmlc                         wmlc;
    application/wasm                                 wasm;
    application/x-7z-compressed                      7z;
    application/x-cocoa                              cco;
    application/x-java-archive-diff                  jardiff;
    application/x-java-jnlp-file                     jnlp;
    application/x-makeself                           run;
    application/x-perl                               pl pm;
    application/x-pilot                              prc pdb;
    application/x-rar-compressed                     rar;
    application/x-redhat-package-manager             rpm;
    application/x-sea                                sea;
    application/x-shockwave-flash                    swf;
    application/x-stuffit                            sit;
    application/x-tcl                                tcl tk;
    application/x-x509-ca-cert                       der pem crt;
    application/x-xpinstall                          xpi;
    application/xhtml+xml                            xhtml;
    application/xspf+xml                             xspf;
    application/zip                                  zip;
    application/octet-stream                         bin exe dll;
    application/octet-stream                         deb;
    application/octet-stream                         dmg;
    application/octet-stream                         iso img;
    application/octet-stream                         msi msp msm;
    audio/midi                                       mid midi kar;
    audio/mpeg                                       mp3;
    audio/ogg                                        ogg;
    audio/x-m4a                                      m4a;
    audio/x-realaudio                                ra;
    video/3gpp                                       3gpp 3gp;
    video/mp2t                                       ts;
    video/mp4                                        mp4;
    video/mpeg                                       mpeg mpg;
    video/quicktime                                  mov;
    video/webm                                       webm;
    video/x-flv                                      flv;
    video/x-m4v                                      m4v;
    video/x-mng                                      mng;
    video/x-ms-asf                                   asx asf;
    video/x-ms-wmv                                   wmv;
    video/x-msvideo                                  avi;
}
# configuration file /etc/nginx/config-version.conf:
server {
    listen unix:/var/lib/nginx/nginx-config-version.sock;
	access_log off;
    location /configVersion {
        return 200 9;
    }
}
map $http_x_expected_config_version $config_version_mismatch {
	"9" "";
	default "mismatch";
}
# configuration file /etc/nginx/tls-passthrough-hosts.conf:
# mapping between TLS Passthrough hosts and unix sockets
www.alibaba.com unix:/var/lib/nginx/passthrough-cmbc_alibaba.sock;
www.baidu.com unix:/var/lib/nginx/passthrough-cmbc_baidu.sock;
www.tencent.com unix:/var/lib/nginx/passthrough-cmbc_tencent.sock;
# configuration file /etc/nginx/stream-conf.d/ts_cmbc_alibaba.conf:
upstream ts_cmbc_alibaba_external-alibaba {
    zone ts_cmbc_alibaba_external-alibaba 512k;
    random two least_conn;
    server www.alibaba.com:443 max_fails=1 fail_timeout=10s max_conns=0 resolve;
}
server {
    listen unix:/var/lib/nginx/passthrough-cmbc_alibaba.sock proxy_protocol;
    set_real_ip_from unix:;
    status_zone www.alibaba.com;
    proxy_pass ts_cmbc_alibaba_external-alibaba;
    proxy_timeout 10m;
    proxy_connect_timeout 60s;
}
# configuration file /etc/nginx/stream-conf.d/ts_cmbc_baidu.conf:
upstream ts_cmbc_baidu_external-baidu {
    zone ts_cmbc_baidu_external-baidu 512k;
    random two least_conn;
    server www.baidu.com:443 max_fails=1 fail_timeout=10s max_conns=0 resolve;
}
server {
    listen unix:/var/lib/nginx/passthrough-cmbc_baidu.sock proxy_protocol;
    set_real_ip_from unix:;
    status_zone www.baidu.com;
    proxy_pass ts_cmbc_baidu_external-baidu;
    proxy_timeout 10m;
    proxy_connect_timeout 60s;
}
# configuration file /etc/nginx/stream-conf.d/ts_cmbc_tencent.conf:
upstream ts_cmbc_tencent_external-tencent {
    zone ts_cmbc_tencent_external-tencent 512k;
    random two least_conn;
    server www.tencent.com:443 max_fails=1 fail_timeout=10s max_conns=0 resolve;
}
server {
    listen unix:/var/lib/nginx/passthrough-cmbc_tencent.sock proxy_protocol;
    set_real_ip_from unix:;
    status_zone www.tencent.com;
    proxy_pass ts_cmbc_tencent_external-tencent;
    proxy_timeout 10m;
    proxy_connect_timeout 60s;
}
